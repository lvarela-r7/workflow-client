<%= form_for(@ticket_scope) do |f| %>
  <% if @ticket_scope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_scope.errors.count, "error") %> prohibited this ticket_scope from being saved:</h2>

      <ul>
      <% @ticket_scope.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
